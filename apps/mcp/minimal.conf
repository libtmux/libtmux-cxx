# Minimal product-dedicated daemon configuration for libtmux-mcp.
set -s exit-empty off
set -g exit-unattached off
set-option -g @libtmux_mcp_owner "$LIBTMUX_MCP_OWNER"
set-environment -gu LIBTMUX_MCP_OWNER
